[pytest]
testpaths =
    yt_discover_tests
    yt_downloader_tests
pythonpath = .
python_files = test_*.py
addopts = -m "not scale and not stress"
markers =
    scale: selected large-dataset tests excluded from routine development and CI
    stress: huge-dataset torture and scalability tests run only when explicitly requested
