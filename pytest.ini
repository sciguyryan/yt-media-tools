[pytest]
testpaths =
    yt_discover_tests
    yt_downloader_tests
python_files = test_*.py
addopts = -m "not stress"
markers =
    stress: large or huge deterministic conformance tests excluded from routine runs
